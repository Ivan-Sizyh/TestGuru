<tr>
  <td><%= test.title %></td>
  <td><%= test.questions.count %></td>
  <td><%= test_level(test) %></td>
  <td><%= test.category.title %></td>
  <td><%= test.author.first_name %></td>
  <td><%= link_to 'Просмотр', admin_test_path(test) %></td>
  <td><%= link_to 'Редактирование', edit_admin_test_path(test) %></td>
  <td><%= link_to 'Удалить',
                  admin_test_path(test),
                  method: :delete,
                  data: { confirm: 'Удалить тест?' } %>
  </td>
</tr>
