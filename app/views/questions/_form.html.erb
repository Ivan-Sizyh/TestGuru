<%= form_with model:[@test, @question], local: true do |f| %>
  <%= render 'shared/errors', resources: @question%>

  <p>
    <%= f.label :body, 'Вопрос' %><br>
    <%= f.text_field :body, id: :question_body%><br>
  </p>

  <p><%= f.submit "Сохранить" %></p>
<% end %>
