<% if resources.errors.present? %>
  <div>
    <h2><%= I18n.translate :errors, count: resources.errors.full_messages.count %> в форме:</h2>

    <ul>
      <% resources.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>